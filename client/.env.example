# URLs usadas pelo navegador, nao pela rede interna do Docker.
# O navegador roda fora dos containers e nao resolve "event-service",
# entao aqui vao sempre as portas publicadas no host.
VITE_EVENT_API_URL=http://localhost:8080
VITE_ALARM_API_URL=http://localhost:8081
